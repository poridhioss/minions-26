# Exclude the local FastAPI/Compose stack from the (no-longer-used)
# Docker image. The Gradio Space boots straight from app.py, so we
# only need the model code + saved_model/ + the Gradio front-end.
Dockerfile
docker-compose.yml
prometheus.yml
grafana/
scripts/check_*.ps1
scripts/probe_queries.ps1
scripts/reload_dashboard.ps1
scripts/payload.json
index.html
app/main.py
.git/
.gitignore
.puku/
__pycache__/
*.pyc
*.log
